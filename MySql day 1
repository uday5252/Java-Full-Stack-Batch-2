create database books_db;

use books_db;

create table books(
id int, 
title varchar(30), 
author varchar(20),
price int,
quantity int
);

insert into books 
values(100, "Python", "James", 400, 3);

insert into books 
values(101, "Java", "Raju", 500, 5);

=======================================================
select * from books;


select * from books where price > 550;

-- display the books whose price is greater than 450 
-- and quantity is greather than 5 
 select * from books where price > 450 and quantity > 5;
 
 select * from books where price > 450 or quantity > 2;

 select * from books where author = "Raju";

-- % ==> ano no of characters 
 select * from books where author like "R%";
 
 select * from books where title like "Ja%";

 select * from books where author like "J%";

select * from books where author like "J_";

select * from books where author like "J_%";

select * from books;

select * from books where author like "J%_n";

select * from books where author like "J%n";

select * from books where author not like "J%n";
==================================================
create database employee_db;

use employee_db;

CREATE TABLE employees (
    employee_id INT PRIMARY KEY,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    department VARCHAR(50),
    salary INT
);

INSERT INTO employees (employee_id, first_name, last_name, department, salary)
VALUES 
(1, 'Alice', 'Johnson', 'Engineering', 75000),
(2, 'Bob', 'Smith', 'Marketing', 62000),
(3, 'Charlie', 'Davis', 'Engineering', 78000),
(4, 'Diana', 'Prince', 'Human Resources', 55000),
(5, 'Edward', 'Norton', 'Sales', 68000);








select * from employees;





-- write a query to display the details of the employees
-- who earns the salary between 70k to 76k

select * from employees where salary >= 70000 and salary <= 76000;

-- between and not between ==> RANGES
select * from employees where salary between 70000 and 76000;
select * from employees where salary not between 70000 and 76000;  


-- display all the employees who belongs to 
-- engineering and sales depart

select * from employees where department = "Engineering" 
or department = "Sales" or department = "HR";

select * from employees 
where department in ("Engineering", "Sales");

select * from employees 
where department not in ("Engineering", "Sales");


select * from employees;

INSERT INTO employees(employee_id, first_name, last_name, salary)
VALUES (6, 'Sam', 'Johnson', 75000);

-- write a query to display the employees who doesnt hav anyd epartment

select * from employees where department is null;



==================================================================================

select * from employees limit 3;
select * from employees limit 4;
select * from employees limit 3 offset 3;
-- total ==> 6
-- want ==> 2,3
select * from employees limit 2 offset 1;


select * from employees; 


select * from employees order by first_name desc;

select * from employees order by salary asc, first_name desc;

-- write a query to display the name of the employee who 
-- has highest salary  
select first_name, last_name from employees order by salary desc limit 1;


-- write a query ti display the first_name of the employee
-- who gets the lowest salary  
select first_name from employees order by salary asc limit 1;

-- write a query to display the first_name and last_name of the employee 
-- who earns 2nd lowest salary in the company 
select first_name, last_name from employees 
order by salary asc limit 1 offset 1;












